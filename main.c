#include "svm.h"

int main(int argc, char *argv[]) {
  Processor proc;
  Memory mem;

  init_processor(&proc);
  init_memory(&mem);

  // Load program from stdin (check to make sure it's not empty)

  // Execute the program

  return 0;
}
