#include "svm.h"

int main(int argc, char *argv[]) {
  Processor proc;
  Memory mem;

  init_processor(&proc);
  init_memory(&mem);

  // Load program from stdin

  // Execute the program

  return 0;
}
